// lib/utils/download_stub.dart

Future<void> downloadTxt(String content, String filename) async {
  // Implementação vazia para plataformas não suportadas.
  // Não faz nada, mas evita erro em testes, etc.
}